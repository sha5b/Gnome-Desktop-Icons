{
  "name": "desktop-icons-50",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-2.0-or-later",
  "type": "module",
  "description": "Lint tooling only. The extension itself is plain GJS and has no npm dependencies at runtime.",
  "scripts": {
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-jsdoc": "^50.0.0"
  }
}
